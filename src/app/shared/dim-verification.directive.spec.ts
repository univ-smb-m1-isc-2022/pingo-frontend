import { DimVerificationDirective } from './dim-verification.directive';

describe('DimVerificationDirective', () => {
  it('should create an instance', () => {
    const directive = new DimVerificationDirective();
    expect(directive).toBeTruthy();
  });
});
